{
    "name": "generative-art",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "clean": "rm -rf dist",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "typescript": "^4.5.4",
        "vite": "^2.9.0"
    },
    "dependencies": {
        "gh-pages": "^3.2.3"
    }
}
